"""Placeholder — the directory name `dreamapi-sidecar` contains a hyphen so
Python won't import this as a package. The sidecar is invoked from inside
this directory via `uvicorn main:app`; this file exists only as a hint to
IDEs and linters that the files here belong together. See README.md.
"""
